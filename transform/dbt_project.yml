name: cadence
version: '1.0.0'
config-version: 2

profile: cadence

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  cadence:
    staging:
      +materialized: view
    marts:
      +materialized: table
